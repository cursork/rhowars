 actions←ns CallTick input;tid;r
⍝ Call a rhobot's Tick function with timeout protection
⍝ Returns actions namespace (direction turret fire state)
 ⎕IO←0
 :Trap 0
     actions←ns.Tick input
 :Else
     ⍝ On error/timeout, bot does nothing
     actions←(direction: input.turret ⋄ turret: input.turret ⋄ fire: 0 ⋄ state: input.state)
 :EndTrap
