 actions←Tick input;centre;dx;dy;vis;rhobots
⍝ Spinner: moves toward centre, rotates turret, fires when ready
 ⎕IO←0
 actions←()
 actions.turret←360|input.turret+15
 actions.fire←1
 ⍝ Head toward centre of arena
 centre←input.arena÷2
 dx←(⊃centre)-⊃input.loc
 dy←(1⊃centre)-1⊃input.loc
 :If 1<(dx*2)+dy*2
     actions.direction←(180÷○1)×12○(0J1×dx)+dy
 :Else
     actions.direction←input.turret
     actions.speed←0
 :EndIf
 ⍝ Thought on notable events
 vis←input.visible
 :If 0<≢vis
     rhobots←(vis[;2]=0)⌿vis
     :If 0<≢rhobots
         actions.thought←'Targets in my spin zone!'
     :EndIf
 :EndIf
 :If input.hp<50
     actions.thought←'Taking damage, still spinning'
 :EndIf
 actions.state←input.state
